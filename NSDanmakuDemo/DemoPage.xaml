<Page
    x:Class="NSDanmakuDemo.DemoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NSDanmaku"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:NSDanmakuDemo.Controls"
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <MediaElement x:Name="mediaPlayer" MediaFailed="mediaPlayer_MediaFailed" CurrentStateChanged="mediaPlayer_CurrentStateChanged" AreTransportControlsEnabled="True">
            <MediaElement.TransportControls>
                <controls:DanmakuMTC x:Name="MTC" 
                                    
                                     IsNextTrackButtonVisible="True" IsPreviousTrackButtonVisible="True"
                                     IsZoomButtonVisible="False"
                                     >
                    
                </controls:DanmakuMTC>
            </MediaElement.TransportControls>
        </MediaElement>
       
        <!--<controls:Danmaku x:Name="danmuControls"></controls:Danmaku>-->
        <AppBarButton Icon="OpenFile"  VerticalAlignment="Center" Label="测试" x:Name="btn_Start" Click="btn_Start_Click"></AppBarButton>
    </Grid>
</Page>
